{
  "name": "{{projectName}}",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "serve": "vite preview"
  },
  "dependencies": {
    "eleva": "{{elevaVersion}}",
    "eleva-router": "{{elevaRouterVersion}}"
  },
  "devDependencies": {
    "terser": "^5.16.1",
    "vite": "^4.3.9"
  }
}
